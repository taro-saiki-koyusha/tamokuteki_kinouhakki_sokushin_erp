# プロジェクト概要: 多面システム（鎌田）
- **プロジェクト名**: tamokuteki_kinouhakki_sokushin_erp
- **目的**: 農業の多面的機能発揮促進事業における、活動記録（写真・参加者・機械・資材）、作業費計算、支払明細書発行、および事務局とのデータ照合などを一元管理するERPシステム。
- **対象組織**: 鎌田緑保護会、農事組合法人カマタ、等

## 🛠 技術スタック
- **フロントエンド**: React, Vite, Tailwind CSS, React Router DOM
- **UI/アイコン**: Lucide React
- **バックエンド/BaaS**: Firebase (Firestore, Authentication, Storage, Cloud Functions)
- **主要ライブラリ**:
  - `xlsx-populate`: Excel帳票の出力、および事務局Excelデータの読み込み・金額照合
  - `jspdf`, `html2canvas`: 画像ベースのPDF生成（※現在は一部機能を除き、ブラウザのネイティブ印刷機能へ移行中）
  - `jszip`, `file-saver`: ファイルのZIP化とダウンロード

## 👥 ユーザー権限 (Roles)
1. **admin (管理者)**: 全ての権限を保持。マスタ管理、ユーザー管理、システム設定、全データ編集・削除。
2. **manager (事務・役員)**: 全データの閲覧、Excel/PDF出力。
3. **reporter (現場リーダー)**: 活動実績の入力・閲覧。特権として「自分の記録のみ編集可」「担当グループの記録を編集可」の付与が可能。

## 📱 主要な画面と機能
- **Dashboard (`Dashboard.jsx`)**: 活動一覧の表示、支払区分別の予算消化状況（グラフ/数値）、各種機能へのハブ画面。
- **活動実績の入力 (`ActivityForm.jsx`)**: 
  - 日時、場所、活動内容、現場写真のアップロード。
  - 参加者（人件費）、使用機械（機械借上費）、使用資材の登録と自動計算。
- **作業費・実績集計 (`CostManagement.jsx`)**: 
  - 年度・グループごとの集計。個人別、グループ別、支払区分別の集計表。
  - 金額セルクリックで該当データのみを抽出表示し、個人の作業明細書（PDF）をブラウザ印刷機能を用いて出力。
- **申請管理・照合 (`ApplicationManagement.jsx`)**: 
  - 指定した振込時期（対象期間）の活動一覧と参加者一覧のタブ切り替え。
  - 事務局から提供されたExcelファイル（日当整理表）を読み込み、システム内の計算金額との差分を自動照合・ハイライト出力。
  - 選択した参加者の支払明細書を一括でPDF（ZIP）化、またはブラウザ印刷機能で一括出力（明細書の発行日指定可能）。
- **ユーザー管理 (`UserManagement.jsx`)**: アカウント作成・編集。農業者区分（13分類）、所属先、担当グループの設定。
- **マスタ・設定管理 (`MasterManagement.jsx`)**: メンバー（基本単価）、機械（利用料）、資材の管理。システム年度、全体の予算、デフォルト値の設定。

## ⚠️ 開発における重要なルールと特記事項
1. **PDF出力（支払明細書など）の実装方針**:
   - 以前は `html2canvas` ＋ `jspdf` を使用していましたが、画面幅による要素の見切れや余白（白紙ページ）の問題が発生したため、現在は**「ブラウザのネイティブ印刷機能 (`window.print()`) と印刷用CSS (`@media print`)」**を使用したアプローチを採用しています。
   - 印刷時は `isPrinting` ステートを `true` にし、画面全体を隠して印刷専用のレイアウト（A4サイズ `210mm`）のみを表示し、`setTimeout` で `window.print()` を呼び出す仕様です。
2. **データの削除**: 
   - 物理削除を行いますが、削除時は必ず `audit_logs` コレクションに操作ログ（誰が何を削除したか）を残す仕様です。
3. **UIの統一**: 
   - Tailwind CSSを使用し、角丸（`rounded-xl` / `rounded-2xl`）、シャドウ（`shadow-sm`）、背景色（`bg-gray-50`, 各種パステルカラー）を多用した、モダンで親しみやすいUIに統一しています。
4. **データの紐付け**:
   - `activities` コレクション内の `participantDetails` には、単価変更履歴の影響を受けないよう、入力当時の `pCost` や `mCost` は持たせず、計算時に `members` マスタ等と突き合わせています（一部の集計処理を除く）。
5. **名称変更の扱い**:
   - 例として「農）カマタ」を画面上「農事組合法人カマタ」と表示するなど、DBの値自体を書き換えるのではなく、フロントエンドの表示側で置換するアプローチを取る場合があります。